---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} px-4 scroll-m-20 w-full mx-auto container lg:max-w-5xl md:max-w-4xl`}
>
  <slot />
</section>
